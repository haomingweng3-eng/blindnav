# 真实视频采集执行单

## 目标

第一阶段只做风险阈值标定和链路演示，不做模型微调。最低目标是 12 段视频、总计 5–8 分钟、至少 20 个有效电动车接近/穿越事件。

## 拍摄规范

- 手机横屏，镜头高度约 1.2–1.4 m，模拟胸挂视角。
- 固定手机姿态，避免边走边大幅甩动；可以正常步行，但不要主动追拍目标。
- 原始视频保留，不要先经过社交软件压缩。
- 每段 20–40 秒，记录实际 FPS、分辨率、时间和地点类型。
- 尽量避免拍到可识别的行人正脸；必要时只保留车辆运动片段并按学校/赛事要求处理隐私。

## 12 段最低清单

| 编号 | 必需场景 | 目标 |
|---|---|---|
| V01 | 迎面接近-慢 | 远到近，速度较稳定 |
| V02 | 迎面接近-快 | 快速接近，验证危险升级 |
| V03 | 迎面接近-遮挡 | 有行人/车辆部分遮挡 |
| V04 | 左向右横穿 | 从左侧切入中央走廊 |
| V05 | 右向左横穿 | 从右侧切入中央走廊 |
| V06 | 远处横穿 | 横向运动但不切入中央走廊 |
| V07 | 左后方超越 | 从左后方接近并超过 |
| V08 | 右后方超越 | 从右后方接近并超过 |
| V09 | 驶离 | 目标面积变小，验证不报警 |
| V10 | 停放/缓行 | 近处但基本静止，验证不过度报警 |
| V11 | 混杂无冲突 | 行人、汽车、自行车同框但不冲向镜头 |
| V12 | 复杂背景 | 路口或商业街，多个目标和遮挡 |

每段如果只有一个有效事件也可以；如果自然出现多个事件，在 manifest 中分别记录数量。

## 文件命名

```text
<日期>_<地点类型>_<场景编号>_<光照>_<序号>.mp4
```

示例：

```text
20260916_crossroad_V04_day_01.mp4
20260916_commercial_V07_dusk_01.mp4
```

视频放在 `data/raw/`，记录表使用 `data/collection_manifest.csv`。不要把原始视频直接提交到代码仓库；提交前只保留经许可的样例或检测框 JSON。

采集过程中随时检查进度：

```bash
python scripts/validate_collection.py -o /tmp/blindnav-collection-status.json
```

只有 `ready: true` 才表示 manifest 中每个视频都已到位、非空且补齐时长和有效事件数。还没开始采集时返回 `ready: false` 是正常结果。

## 采完立刻做的检查

1. 用播放器确认文件能从头播放，文件大小不是 0。
2. 检查横屏、分辨率和 FPS 是否一致或已记录。
3. 记下每段的有效事件数量，以及是否出现迎面/横穿/超越。
4. 先跑检测和回放，再决定是否需要抽帧标注；不要一开始就全量标注。
5. 在 `PROJECT_STATUS.md` 中只填有原始文件或 JSON 报告支持的数字。

每段视频的推荐处理顺序：

```bash
python scripts/detect_video.py data/raw/<视频文件>.mp4 \
  runs/<视频编号>_risk.json 0.06
python scripts/calibrate_reports.py runs/<视频编号>_risk.json \
  -o runs/<视频编号>_thresholds.json
```

第一条命令只跑一次检测；第二条命令可以反复修改阈值而不重新跑模型。

如果多段视频已经全部放入 `data/raw/`，可直接批量处理：

```bash
python scripts/batch_detect_videos.py \
  --raw-dir data/raw \
  --output-dir runs/video_reports \
  --device mps
```

它会为每段视频生成同名 JSON，并在 `summary.json` 汇总处理成功、失败、轨迹数和告警数。

## 第二阶段（有余力再做）

若要微调检测器，再从 20–30 分钟视频中按约 2 FPS 抽帧，去重后保留 800–1500 张代表性图片做框标注。第一阶段不要为了凑标注量拍大量同质连续帧。
