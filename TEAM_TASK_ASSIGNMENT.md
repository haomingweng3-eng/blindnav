# 四人近期任务分工（当前版本）

这份文档覆盖之前零散的任务分配。近期只做两件大事：

1. 把视频数据集补齐并标注好；
2. 把检测模型、追踪和告警方案训练、测试、横向比较好。

暂时不扩展地图、SLAM、复杂硬件和新的 Android 功能。手机端已有代码先保留，等 A/B/C 结果稳定后再做实测。

## 1号：拍摄视频，负责原始数据

### 具体要做

- 再拍 20–30 段视频，现有 13 段安全视频也要保留；
- 每段 8–20 秒，横屏、约 30 FPS、1080p 优先；
- 摄像头固定在胸前或脖子下方，模拟最终使用视角；
- 不开滤镜、不变焦、不剪辑，保留原始视频；
- 不在开放道路上故意制造危险，使用封闭场地或由队友提前刹停/绕行；
- 拍摄电动车、摩托车、行人、自行车；
- 必须覆盖迎面接近、左右横穿、平行经过、目标远离、多目标、遮挡、转弯和不同光照。

### 每段视频必须说明

- 视频编号和文件名；
- 地点类型和光照；
- 目标类型；
- 是否需要拍摄者避让；
- 是安全通过、接近后避开，还是确实进入行走路径；
- 是否有多个目标。

### 交付物

```text
视频文件
data/collection_manifest.csv 更新内容
每段视频的简单说明
```

不要只把视频丢进群里，不说明场景就算完成。

## 2号：整理和标注数据

### 具体要做

- 检查 1号的视频能否播放、分辨率和 FPS 是否正确；
- 给每段视频编号并整理到清单；
- 标记风险事件：`safe_pass`、`near_miss`、`conflict`；
- 记录目标开始接近的帧，以及开始需要避让的帧；
- 抽帧给目标画检测框；
- 统一类别为 `electric_bicycle`、`person`、`bicycle`、`motorcycle`；
- 删除重复的连续帧，保留接近、横穿、遮挡和转弯等关键画面；
- 按完整视频划分训练集、验证集、测试集，不能按单张图片随机拆分。

### 数据划分

```text
训练集：60%
验证集：20%
测试集：20%
```

同一段视频的相邻帧必须放在同一个集合里，否则测试结果会虚高。

### 交付物

```text
annotations/*.json
检测框数据集
类别映射表
train/val/test 视频清单
每类目标数量统计
```

视频事件标注和检测框标注是两回事。只有视频、没有框，不能拿去训练检测模型。

## 3号：训练和调整模型

### 具体要做

- 先用当前 `models/yolov8n.pt` 跑基线；
- 再用公开数据集（优先 ScooterDet）训练第一版；
- 加入 2号标注的本地胸前视角数据继续训练；
- 调整模型大小、输入尺寸、置信度和训练轮数；
- 对比原始模型和新模型在同一测试集上的结果；
- 测试电动车、行人、摩托车和自行车是否稳定检测；
- 导出模型前检查 ONNX 兼容性，不要直接覆盖现有模型。

### 必须统计

- precision；
- recall；
- mAP50；
- mAP50-95；
- 每类目标指标；
- 漏检和误检图片；
- 推理 FPS；
- 模型大小。

### 交付物

```text
data.yaml
训练日志
best.pt
权重 SHA-256
测试集指标
预测可视化
失败案例说明
```

不要只说“模型效果变好了”，必须给出原始 YOLO 和新模型的对比数字。

## 4号：开源项目横向调研和实测

### 具体要做

重点查和测试这些方向：

- 目标检测：YOLOv8/YOLO11、YOLOX、NanoDet、RT-DETR；
- 多目标追踪：ByteTrack、BoT-SORT；
- 视觉风险：光流、深度估计、单目距离、路径冲突判断；
- 助盲导航和视觉避障开源项目；
- Android/ONNX/ncnn 部署方案。

### 每个项目必须回答

- 它解决什么问题；
- 是否适合胸前视角；
- 是否能处理电动车和多目标；
- 模型大小和运行速度；
- 是否能转 ONNX 或部署 Android；
- 许可证是否允许比赛和后续使用；
- 是否真的跑过，还是只看过论文指标。

### 实测要求

选 2–3 个最值得尝试的方案，用同一批视频和同一套指标测试：

```text
检测效果
追踪稳定性
误报和漏报
预警提前量
FPS
模型大小
部署难度
```

### 交付物

```text
开源项目对比表
项目链接和许可证
实际运行命令
测试结果
推荐方案
不采用某方案的原因
```

可以借鉴和整合开源项目，但必须保留项目名称、链接和许可证，明确哪些是开源能力、哪些是本项目改进，不能把别人的成果写成原创。

## 四个人之间怎么衔接

```text
1号拍视频
  ↓
2号整理、事件标注、画检测框
  ↓
3号训练模型并输出检测结果
  ↓
4号用统一测试集做横向比较
  ↓
四个人一起确定最终模型和告警方案
```

3号可以先用现有 13 段视频和公开数据开始，不能等所有新视频拍完才开始。

## 近期完成标准

四个人都必须留下可检查的结果：

- 1号有新视频和场景记录；
- 2号有事件标注、检测框和数据划分；
- 3号有训练模型和对比指标；
- 4号有开源方案实测表和推荐结论。

最终汇报至少要能回答四个问题：

1. 我们用什么视频测试的？
2. 模型识别电动车到底准不准？
3. 目标很多时会不会反复报警？
4. 为什么选这个模型，而不是其他开源方案？

## 现在不要做

- 不要四个人一起继续泛泛搜索资料；
- 不要把没有标注的视频直接当训练数据；
- 不要把同一视频的帧拆到训练集和测试集；
- 不要只看公开数据集的 mAP 就宣称真实道路有效；
- 不要为了降低告警数量直接删除困难样本；
- 不要把手机端新功能放在 A/B/C 之前。

相关细节见：

- `DATASET_HANDOFF.md`：数据集和测试交接；
- `DATA_COLLECTION.md`：视频采集规范；
- `MODEL_TRAINING_HANDOFF.md`：模型训练流程；
- `RELATED_PROJECTS_COMPARISON.md`：开源项目横向资料。
