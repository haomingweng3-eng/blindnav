# 视频归档与技术交接

**归档日期**：2026-09-19  
**用途**：保存当前阶段的真实道路安全距离视频，供队友复现检测、追踪、风险引擎和告警仲裁结果。

## 当前结论

- 共 13 段视频，约 107.033 秒，约 17 MB。
- 用户确认这些画面均处于安全距离，没有可标注的危险事件。
- 因此它们是“无冲突基线”，可以用于测量误报告警、回放稳定性和代码回归，不能用于计算真实事件召回率、漏报率或预警提前量。
- 当前 YOLOv8n + ByteTrack + 风险引擎的初始回放为 206 次告警，即 115.4784 次/分钟。
- 加入走廊约束和告警仲裁后，同一批原始检测记录为 117 次原始告警、81 次可播放告警，即 45.4066 次/分钟；问题有下降但仍未解决。
- 新增第一道 `min_approach_area=0.01` 小目标确认门后，在同一批原始检测记录上降为 34 次原始告警、21 次可播放告警，即 11.7721 次/分钟；汽车告警从 50 次降为 2 次。再将普通告警预算从“类别+方向”收紧为“方向级”后，同批回放为 20 次可播放告警，即 11.2150 次/分钟；危险级告警不受合并。该门和预算显著抑制远处车辆/视角运动引发的告警，但 11.2 次/分钟仍不合格，不能替代正向危险事件标注和阈值校准。
- 多目标策略：同一帧的不同目标仍可同时进入反馈，避免漏掉并行风险；跨帧的普通告警共享左/前/右方向通道预算；危险告警保留抢占。该策略解决的是反馈刷屏，不是检测器本身的误检。
- 2026-09-19 对 13 段视频做了稀疏光流实验。告警帧的整体运动在不同视频中方向不一致，暂不把光流阈值作为告警抑制条件。

## 复现入口

```bash
# 检查视频是否齐全、元数据是否完整
/Users/mima0000/miniconda3/envs/pt/bin/python scripts/validate_collection.py

# 批量生成检测报告（需要本地模型和依赖）
/Users/mima0000/miniconda3/envs/pt/bin/python scripts/batch_detect_videos.py

# 检查测试、语法、验收和 Git 状态
/Users/mima0000/miniconda3/envs/pt/bin/python scripts/submission_audit.py
```

视频原文件位于 `data/raw/`。完整文件名、大小、时长和 SHA-256 见 `data/raw/manifest.csv`。

## 后续采集建议

下一批应优先补“可标注事件”，而不是继续增加同类安全距离视频：

1. 行走者前方有电动车从左/右侧横穿，保持安全、可随时停止；
2. 电动车从远处同向或逆向接近，但不进入危险距离；
3. 同一场景分别拍摄稳定机位和轻微自然晃动版本；
4. 每段视频单独记录开始时间、场景、运动方向、是否冲突和人工事件窗口。

所有危险场景必须由成年人在封闭或可控环境中完成，不能为了采集数据进入真实危险距离。
