# 外部公开数据集测试记录

## 测试日期

2026-09-16

## 选择的数据集：ScooterDet

- 来源：[Zenodo Object Detection for E-scooters](https://zenodo.org/records/10578641)
- 代码：[DongChen06/ScooterDet](https://github.com/DongChen06/ScooterDet)
- 数据说明：2013 张图、11 类交通目标、约 11,011 个框；数据来自安装在 Segway NineBot 上的 Tobii Pro Glasses 2，属于穿戴式/骑行视角。
- 许可：Zenodo 元数据标记为 CC BY 4.0。
- 临时下载位置：`/tmp/blindnav-external/scooterdet.zip`，没有放入项目仓库。

它不是胸挂电动车数据，也不是中国道路场景，但比普通车载、监控或无人机数据更接近本项目的移动视角，因此先作为外部回归样本。

## 实测方法

从压缩包中抽取 10 张标注包含 `scooter` 的图片，使用当前项目的 `models/yolov8n.pt`（COCO 预训练模型，MPS）推理：

- 正常检测阈值 `conf=0.25`；
- 另用 `conf=0.01` 检查是否只是置信度偏低；
- 只统计 COCO 中与本项目相关的 `bicycle`、`motorcycle`、`person` 类别。

## 结果

| 项目 | 结果 |
|---|---:|
| 抽样图片 | 10 |
| scooter 标注目标 | 10 |
| `conf=0.25` 检出为 bicycle/motorcycle 的图片 | 0/10 |
| `conf=0.01` 出现 bicycle/motorcycle 的图片 | 0/10 |
| 主要误识别 | car、person，或没有相关目标 |

## 结论

1. **不能直接拿这个公开集给当前 COCO 模型做风险回放**：单帧检测就没有稳定产出目标框，后续 ByteTrack/looming 没有可靠输入。
2. **它仍然适合做微调候选集**：有 scooter 框标注，且视角来自移动穿戴设备；但必须先确认类别定义、图像内容和项目需要的电动车外形是否一致。
3. **它不能替代明天的胸挂电动车视频**：样本主要是海外道路/滑板车场景，不覆盖中国电动车、近距离横穿和胸口高度视角。
4. 当前最合理的顺序是：先用明天视频标定风险趋势，再考虑把 ScooterDet 作为预训练补充，最后用本地样本做少量微调。

## 最小微调实验

为确认它是否能快速补足检测能力，做了一个不进入主模型的单类 `scooter` 微调实验：

- 正样本按场景划分为 34 张训练图、10 张盲测图；另加入少量无 scooter 背景图。
- 基础模型为项目现有 `yolov8n.pt`，MPS 训练 10 epochs。
- 权重只保存在 `/tmp/blindnav-external/train-runs/`，没有覆盖项目模型。

盲测结果：

| 推理置信度 | 10 张盲测中有输出的图片 | 总候选框 |
|---:|---:|---:|
| 0.25 | 0/10 | 0 |
| 0.05 | 0/10 | 0 |
| 0.01 | 10/10 | 1117 |

`conf=0.01` 的结果是低置信度泛检，不可用于实时告警。这个实验不说明微调路线无效，只说明 44 张含 scooter 的小样本不足以得到可用模型，且不能替代中国电动车/胸挂视角数据。

为便于后续换自定义模型，项目风险链路现在接受 `scooter`、`electric_bicycle`、`electric_bike` 和 `e-bike` 类别；其中 `scooter` 会生成“电动滑板车”的反馈语音。

## 额外测试：绕过检测器，直接喂真实标注框

为区分“检测失败”和“风险引擎失败”，又取了 `frame_16650/16660/16670/16680` 四个连续标注帧，把标注的 scooter 框暂时映射为项目的 `bicycle` 类，并人为赋予同一个轨迹 ID。这个映射只用于验证风险算法，不代表模型已经识别出 scooter。

阈值扫描结果：

| looming 阈值 | 告警 | 首次告警帧 | looming |
|---:|---:|---:|---:|
| 0.02 | 1 | 16680 | 0.037 |
| 0.03 | 1 | 16680 | 0.037 |
| 0.04 | 0 | — | — |
| 0.06 | 0 | — | — |

这说明：真实标注框的面积变化能被风险引擎利用，但该四帧序列在当前 30 FPS 参考单位下的增长率约为 0.037，默认 0.06 对它偏高。由于只有四帧、目标身份是人工串联、数据是滑板车而非中国电动车，不能据此直接修改生产阈值；明天的胸挂视频仍需重新标定。

## 其他候选集判断

- **MicroVision**：官方页面描述为 8,000+ 张、覆盖行人/自行车/骑行者/e-scooter 的 VRU 视角数据；但其研究数据页面目前显示数据访问受限/该版本 withdrawn，不能把它当成明天可直接下载的方案。
- **TGSIM Foggy Bottom**：美国交通部公开的轨迹数据包含 bicycle、scooter、motorcycle 等，并有速度/加速度；但来自固定基础设施摄像头，主要是 CSV 轨迹而非胸挂图像，适合以后验证运动模型，不适合当前检测器视觉测试。
- **VisDrone/BDD100K**：可以做通用交通目标基线，但视角和类别定义与本项目差异较大，不作为电动车数据替代品。

## 补充：ONNX 单图 smoke test（非数据集指标）

为验证新写的 ONNX 预处理/解码/NMS 适配器，另取一张 Wikimedia Commons 的公开 scooter 图片 [Copy scooter 01.jpg](https://commons.wikimedia.org/wiki/File:Copy_scooter_01.jpg)，页面标注许可为 CC BY-SA 2.0。该图片是静态侧视图，不是胸挂视角，也不是电动自行车视频样本；原图未提交到仓库，仅下载到临时目录用于一次回归。

在 `models/yolov8n.onnx`、CPU ONNX Runtime、letterbox 640、按类别 NMS 下：

| conf | 输出 |
|---|---|
| 0.25 | 1 个 `motorcycle`，置信度 0.836195 |
| 0.05 | 1 个 `motorcycle`，置信度 0.836195 |
| 0.01 | 7 个候选框，出现低置信度 person/motorcycle 泛检 |

这只证明 ONNX 适配器在一张公开静态图上能正常加载、解码并输出类别；不能推出 scooter/电动车召回率、视频稳定性或真实场景准确率。
