# 项目交接：视障出行动态预判与主动导航系统

**日期**：2026-09-15
**赛事**：软件智能创新大赛（A类赛事，公益赛道，目标国一）
**当前阶段**：技术验证完成 60%，核心链路已跑通，卡在电动车数据采集

---

## 一、项目定位（一句话）

胸挂式广角摄像头 + 手机端实时 AI：YOLO 检测 → ByteTrack 追踪 → 轨迹预判 → 分级震动+空间化语音预警，叠加室内外全程步行导航。

**核心差异化**：把助盲工具从「被动拍照识别、事后播报」升级为「无感持续感知 + 事前动态预判 + 全程主动引导」。传统方案（OrCam/eSight/Soundscape）全是「识别/循迹」，没有「预测碰撞」。

---

## 二、三路调研结论（2026-09-15，搜索服务当日失效，凭知识储备给出，引用前需复核）

### 痛点与竞品
- **最佳文案抓手是政策空白**：美国 FMVSS No.141 / 欧盟 AVAS 强制电动车低速发声，但**只管汽车不管电动自行车**。这是真实、可查、不怕追问的切入点
- **别编伤亡数字**：「每年X万盲人被撞」无公开统计支撑（视力障碍不是事故记录字段），一查就穿
- **竞品失败点**：OrCam=贵价读字机（3000-4000美元，识别≠导航）；eSight只对残余视力有效；微软Soundscape已停更（3D音频方向对但无持续支持）；NaviLens死依赖基础设施铺设
- **用户真实需求排序**：安全感 > 独立性 > 信息量。最怕「突然且无声的近身物体」；最大抱怨是**误报和信息过载**（连续播报比不播更糟，会淹没关键听觉线索）
- **三个伪需求红线**：「让盲人重新看见」「100%避障」「取代导盲犬/盲杖」——最后一条视障社群和O&M训练师明确反对，必须说**辅助而非替代**

### 技术口径（三处必须改，否则网评被问穿）
1. **单目测距放弃绝对距离**：行人3-10m相对误差15-30%，电动车40-50%。MiDaS/Depth Anything是相对深度且尺度不确定，ViT-L无法与YOLO并行跑25fps。**改做「近/中/远三级+快速接近/横向穿过」趋势判断**（检测框面积变化率looming+光流）
2. **TTC对横向穿行目标失效**（趋于无穷），改**路径交点预测**：胸挂相机高度+俯角稳定可标定，单应矩阵投地面预测轨迹相交
3. **「≤200ms延迟」不能包含语音播报**：TTS冷启动100-300ms+念完1s。口径=「检测→追踪→预判→触发震动≤200ms」，高紧急档先短促提示音，语音随后

### 其他技术判断
- **COCO无「电动车」类**（只有bicycle/motorcycle，国内电动车外观两者都不像）→ **必须自采胸挂视角电动车数据微调**，这是唯一技术壁垒
- **3-5秒预判只能当粗感知**：电动车5-7m/s，3秒走15-21m，转向误差2-5m。做两级：长视界2-3s方向提示，短视界0.8-1.5s触发避障。匀速卡尔曼恰在转向时失效，用CTRV/IMM更诚实
- **防网评一句话**：不宣称绝对距离和精确碰撞时间，宣称**分级风险趋势+路径冲突预判**

### 可复用开源
- **Tencent/ncnn**官方yolov8n Android示例（CameraX+JNI+渲染）= Demo起点骨架
- **ifzhang/ByteTrack**社区C++/NCNN移植版（搜ByteTrack_NCNN），仅依赖OpenCV，省事最多
- **步行导航用高德/百度SDK**，别自建OSM/GraphHopper（时间黑洞）
- 语音用Android原生TextToSpeech(zh_CN)；空间音频先用立体声平移顶上，Resonance Audio作加分项
- **室内导航砍掉**（硬件+场地不可控），PPT一页路线图交代
- **无现成行人/两轮车TTC开源库**（openpilot不含行人AEB），自己写地面平面针孔成像 `Z=f·h/(v−v_horizon)`——恰是创新点
- 助盲可穿戴方向**无高质量开源项目可抄**（WeWALK闭源，GitHub上多是学生毕设）
- 根治单目测距可上Intel RealSense深度相机，代价是形态和成本

---

## 三、已完成的工程验证（代码在 ~/blindnav/）

### 环境
- conda环境`pt`（Python 3.11.16 + torch 2.14.0，MPS可用）
- 安装ultralytics 8.4.152 + opencv 5.0.0.93 + onnx
- 项目目录：`~/blindnav/{data/raw,models,scripts,runs}`

### 性能基准

> 以下三项是此前运行留下的历史记录；本次环境检查确认模型依赖可用，但原始视频/报告文件已不在当前工作区，因此答辩前应重新跑一遍并以新 JSON 为准。

| 项目 | 结果 |
|---|---|
| YOLOv8n CPU 640x640 | 60.8ms → 16.5fps |
| YOLOv8n MPS 640x640 | 12.2ms → **81.7fps** |
| YOLOv8n MPS 448x448 | 12.9ms → 77.3fps |
| NCNN导出 | 成功，12.2MB，导出后推理验证通过 |

### 风险引擎原型（scripts/risk_engine.py）
逻辑：不依赖绝对距离，用检测框面积变化率(looming)+横向位移分级。

四个合成场景全部判对：
- **电动车直冲**：第3帧「警告」，第8帧「危险」（提前量出来了）
- **横向穿过**：触发「警告」+冷却后重复提醒，不刷屏
- **静止行人**：不报警
- **目标远去**：完全无视（looming为负）

分级逻辑：
- looming>0.06 且 close_high(area_n>0.06) → 危险（快速接近且近距离）
- looming>0.06 → 警告（快速接近）
- \|lateral\|>0.02 且 close_low(area_n>0.015) → 警告（横向穿过）
- close_high → 提示（近距离静态）
- 冷却5帧防反复播报

### 实时管线（scripts/live_demo.py）
- **OpenCV直连摄像头被macOS TCC拦截**（PermissionError），改用ffmpeg管道喂帧
- ffmpeg采集1280x720 rawvideo → Python后台线程读流 → deque(maxlen=1)只保留最新帧 → YOLO MPS推理 → cv2.imshow显示
- 历史实测：CPU模式23.3fps（卡），MPS模式**66.8fps**（流畅）；原始视频和报告当前不在工作区，不能视为本轮复现结果
- 历史记录：真实画面450帧检出person 682次；当前没有对应原始视频，不能用于真实场景准确率宣称
- **注意**：640x480分辨率FaceTime摄像头不支持，会卡死采集

### 已验证结论
- 检测→追踪→looming分级→预警 这条链路在参数化仿真和检测框回放中**可复现**；历史真实画面记录仍需重新采集/复跑
- 不需要绝对距离就能区分「冲你来」「横穿」「远离」
- **但 looming阈值0.06 是合成数据拍的**，真实电动车逼近时是多少未知，必须用真数据重标

---

## 四、当前卡点（核心问题）

**电动车数据问题，这是唯一卡住Demo效果的瓶颈，也是项目的技术壁垒所在。**

现状：
1. **GitHub实搜结果**：`syy-zt/Electric-Bicycles`（0 star，质量不可信）；头盔检测类仓库（imJouch/helmet-detection 12star、QQ767172261 7star）有电动车图片但是**监控/抓拍视角**且标注的是头盔不是车辆位置
2. **公开数据集（BDD100K/VisDrone/COCO）全是监控或车规视角**，胸挂视角的电动车数据集**全世界都没有**——因为这个形态的产品还没人做过
3. **视角是致命的**：胸挂1.2-1.4m略带俯角，电动车的尺度、遮挡、外观与监控视角完全不同，用错视角数据微调会认不出贴身掠过的电动车

### 解决方案（分两步，第二步不需要标注）
**第一步：风险标定（不需要任何标注数据，只需要视频）**
- 手机举胸口高度录20-30分钟路口电动车视频
- 跑追踪测真实looming分布，标定阈值
- 这步解决「演示时预警响不响」

**第二步：检测微调（需要标注）**
- 公开数据集bicycle/motorcycle做预训练底座
- 自采胸挂视角电动车微调（目标800-1500张标注帧）
- Roboflow/LabelImg标注，30分钟视频@2fps抽帧≈3600张够用

### 采集清单
视角：手机举胸口高度(1.2-1.4m)，横屏，固定机位别晃
场景：
1. 横向横穿（最重要，路口红灯前）
2. 迎面驶来（直冲镜头）
3. 背后超越
4. 外卖骑手（最危险场景）
5. 距离有近有远，白天傍晚各一段
地点：学校门十字路口、外卖密集商业街、地铁站口

---

## 五、待确认事项

1. **赛事信息**：软件智能创新大赛的时间节点、材料要求、是否禁纯PPT——**一律未确认**（此前误套用了海之子杯的节点，已作废）
2. 报名/组队状态、队伍技术底子
3. 是否有「项目二」在选题（用户只发了「项目一」）
4. 安卓测试机（骁龙7/8系）是否到位——ncnn模型已导好，等真机验证25fps指标
5. 胸挂硬件本体方案（3D打印外壳+手机固定）

补充审计说明：评审意见中提到的 `10-调研总报告.md`、`14-专利复核记录.md` 等旧调研文件不在当前 `/Users/mima0000/blindnav` 工作区，因此本仓库无法替它们完成口径同步或引用复核；当前仓库内的 README、交接文档和演示口径已明确区分“已验证”“待验证”和“不能宣称”的内容。

---

## 六、给评审AI的提问

请重点审查：
1. 单目放弃绝对距离、改「分级风险趋势+路径冲突预判」的定位，是否真的是单目原理上唯一自洽的方案？有没有更好的技术路线被漏掉？
2. 风险引擎的looming阈值设计（面积几何平均增长率）是否稳健？合成数据0.56 vs 真实场景可能0.02-0.03的量级差距，是否说明这个特征本身不够鲁棒？
3. 5周冲国一，「自采电动车数据+用户验证」这两件事的优先级排序是否正确？
4. 有没有我们漏掉的开源方案或数据来源能显著降低自采成本？
5. 竞品分析里「预测碰撞是空白带」的判断是否成立？是否存在我们不知道的同类产品？

---

## 七、截至2026-09-16的本地代码执行结果

本节覆盖本轮实际修改和验证，优先级高于前文中对旧脚本行为的描述。

### 风险引擎

- `scripts/risk_engine.py` 已改为连续窗口 `log(area)` 增长率中位数，降低单帧检测框突变造成的误报。
- looming 和横向位移都按真实帧间隔归一化；横向特征使用单位帧位移率，不再使用历史首帧到当前帧的累计位移。
- looming 和横向速度进一步统一换算到 30 FPS 参考单位；离线视频使用源 FPS，实时 rawvideo 使用 `--input-fps`，避免同一运动因视频帧率不同而改变阈值含义。
- 横向风险增加了简化行走走廊冲突判断：目标只有在预测轨迹可能切入图像中央走廊时才升级为横向告警；这不是相机标定后的真实地面交点计算。
- `TrackState` 支持传入 `looming_threshold`，不再把 `0.06` 视为不可调的物理常数。
- 阈值扫描在当前参数化仿真中显示：`0.04-0.08` 无误报/漏报；`0.02-0.03` 会误报慢速接近，`0.10` 会漏报临界接近。该结果不等于真实道路最佳阈值。

### 检测、追踪和实时管线

- `scripts/detect_video.py` 和 `scripts/live_demo.py` 均使用 Ultralytics 官方 ByteTrack，不再使用旧的最近中心点匹配。
- `detect_video.py` 支持将检测、轨迹和告警输出为 JSON。
- 告警 JSON 已包含平台无关的 `feedback` 策略：低风险短震动、警告提示音加延后语音、危险强震动加危险音和紧急语音；具体 Android API 尚未接入。
- 已补充 [ANDROID_INTEGRATION.md](ANDROID_INTEGRATION.md)，冻结 `feedback` 的 JSON 字段、三档动作策略、去重建议和安卓 Dispatcher 伪代码；工作区当前没有 Android Studio 工程。
- 已补充 [DEMO_RUNBOOK.md](DEMO_RUNBOOK.md)，提供无真实电动车视频时的三分钟演示顺序、命令、可展示字段和答辩边界口径。
- 已补充 [SUBMISSION_CHECKLIST.md](SUBMISSION_CHECKLIST.md)，区分当前可验证证据、现场演示步骤和不能对外宣称的指标。
- 已补充 [DATA_COLLECTION.md](DATA_COLLECTION.md) 和 `data/collection_manifest.csv`，把最低 12 段真实视频采集拆成可执行场景清单和记录表。
- 已补充 `scripts/validate_collection.py`，能检查 manifest、原始文件缺失/空文件和采集元数据是否齐全；当前尚未填入真实视频，因此验收状态应为 `ready: false`。
- `detect_video.py` 现在保留原始检测 `records`；新增 `scripts/calibrate_reports.py`，可在不重复运行 YOLO 的情况下对真实视频报告扫描多个 looming 阈值。
- 已实测公开 ScooterDet 抽样：10 张含 scooter 标注的穿戴式视角图片，COCO YOLOv8n 在 `conf=0.25` 和 `0.01` 下均未稳定输出 bicycle/motorcycle；结果和数据集许可/局限见 [EXTERNAL_DATASET_TEST.md](EXTERNAL_DATASET_TEST.md)。
- 进一步用 ScooterDet 四个连续真实标注框绕过检测器测试风险层：`looming=0.037`，阈值 `0.02/0.03` 在末帧告警，`0.04/0.06` 不告警；这只证明风险层能消费真实框，不证明检测器已识别 scooter，也不足以改生产阈值。
- ScooterDet 最小微调实验未形成可用模型：34 张训练正样本、10 张盲测图，`conf=0.25` 检出 0/10；`conf=0.01` 虽覆盖 10/10，但产生 1117 个候选框，属于低置信度泛检。未覆盖项目模型，权重仅在临时目录。
- 风险链路已兼容自定义微出行类别 `scooter`、`electric_bicycle`、`electric_bike` 和 `e-bike`，不会因类别白名单被静默丢弃；`scooter` 反馈语音为“电动滑板车”。
- `detect_video.py` 已支持 `--model`、`--device`、`--conf`，保留原有位置参数；用公开集抽样视频做过一次 CLI/JSON 回归，报告含原始 `records`。
- 新增 `scripts/batch_detect_videos.py`，可扫描 `data/raw/` 批量生成同名报告和 `summary.json`；已用公开样本视频做过一段端到端回归。
- 新增 `scripts/evaluate_events.py`，按人工事件窗口计算事件召回率、预警提前量和每分钟误报告警数；真实视频到位后可直接生成实验指标，不再只看告警总数。
- 新增 `scripts/batch_evaluate_events.py`，可按同名标注/报告批量汇总 12 段视频的加权召回率、总体提前量和误报告警率，并对缺失报告报错。
- 批量评估可同时导出逐视频与 `TOTAL` 总计 CSV，便于直接生成 Excel/PPT 实验表，减少手工转录错误。
- 事件汇总新增保守 P10 预警提前量；性能基准新增 P95≤200ms 工程门槛字段，避免只报告平均值或中位数。
- 新增 `scripts/prepare_annotations.py` 自动生成事件标注模板；模板默认未完成，批量评估会显式报告 `invalid_annotations`，防止空标注污染实验结果。
- 新增 `scripts/submission_audit.py` 作为提交门禁，统一检查测试、语法、核心验收、采集就绪状态和 Git 清洁度；当前真实视频未到位时会明确返回 `collection_not_ready`。
- 新增 `scripts/check_environment.py`，离线核验 `requirements.txt` 中依赖是否存在并满足版本约束；当前 pt 环境 7 项依赖均通过。
- 已将 YOLOv8n 导出为 `models/yolov8n.onnx`，固定输入 `1×3×640×640`、opset 12，并通过 ONNX checker 与 CPU 空白帧推理回归；这仍不是 Android Runtime 或骁龙真机验证。
- 新增 `scripts/onnx_inference.py`，冻结 ONNX 的 letterbox、输出解码和按类别 NMS 参考行为，并用真实模型与合成框测试；可作为 Android 移植的数值对照基线。
- 新增 `scripts/benchmark_onnx.py`；当前 Apple M1 CPU 实测 15 帧/3 帧预热，ONNX Runtime 平均 38.57ms、P95 40.38ms、25.93 FPS，仅代表模型推理层，不代表安卓真机端到端性能。
- 已初始化 Git 仓库并建立基线提交 `606a613`；`.gitignore` 排除了原始视频和运行输出，便于后续追踪实验版本。
- 告警语音支持左侧/前方/右侧三级方向词，不播报未经标定的精确距离。
- `live_demo.py` 支持 `--headless`、`--max-frames`、`--device`、`--input-fps`、`--looming-threshold` 和 `--report`，可以用 rawvideo stdin 做无摄像头权限的回归测试并保存 JSON 证据。
- 实时采集队列保留源视频帧号；即使丢帧，风险引擎仍按源帧号计算间隔。

### 已完成的可复现验证

- 单元测试：`40 tests ... OK`。
- 一键验收：`scripts/acceptance_check.py` 返回 `passed: true`，同时覆盖 10 个参数化场景和安卓反馈回放。
- YOLO + ByteTrack 合成视频回放：45帧、5个轨迹、默认阈值 `0.06` 下0次告警。
- headless 实时 rawvideo 回放：处理45帧、源帧到45、6个轨迹、0次告警，JSON报告字段断言通过；运行内最近FPS只作监控，正式性能以基准脚本为准。
- 历史 YOLO + ByteTrack 性能基准：有效测量40帧，平均32.91ms，P95 37.52ms，稳定约30.39 FPS；测试设备为当前 Apple Silicon MPS，不代表安卓设备。原始基准视频/JSON 当前不在工作区，提交前需重新生成并附报告。
- `scripts/replay_detections.py` 支持不依赖 YOLO 的检测记录回放，适合接入公开视频或其他检测器输出。

### 仍未完成、不能对外宣称的内容

- 没有真实胸挂视角电动车数据，因此没有真实场景召回率、误报率、漏报率或预警提前量证据。
- 合成视频只验证软件链路，不能证明电动车风险识别有效。
- 当前性能数据不是骁龙7/8系安卓数据，仍需真机验证。
- 没有完成视障用户或O&M专业人员的交互验证。
- 未验证真实摄像头权限、长时间发热、手机端音频/震动抢占和导航播报打断策略。
